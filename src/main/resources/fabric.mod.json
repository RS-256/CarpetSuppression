{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${name}",
  "description": "A template Carpet extension mod.",
  "authors": [
    "Me!"
  ],
  "contact": {
    "sources": "https://github.com/RS-256/CarpetSuppression"
  },

  "license": "CC0-1.0",
  "icon": "assets/carpetSuppression/icon.png",

  "environment": "*",
  "entrypoints": {
    "main": [
    ]
  },
  "mixins": [
    "carpetSuppression.mixins.json"
  ],

  "accessWidener": "${accesswidener}",
  "depends": {
    "minecraft": "${minecraft}",
    "fabricloader": "${fabricLoader}",
    "${fabricApiKey}": "${fabricAPI}",
    "carpet": "${carpet}"
  }
}
